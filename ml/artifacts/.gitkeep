# This directory holds trained model artifacts (model.json, explainer.pkl, metrics.json).
# These files are generated by ml/train.py and ml/evaluate.py.
# Commit model.json and explainer.pkl to the repo if they are small (<50MB),
# otherwise attach them as GitHub Release assets and download at Render build time.
